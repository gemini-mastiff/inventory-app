<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('header') %>
    <div class="form-container width-container">
      <form class="form" action="/albums/<%= album.id %>/delete" method="POST">
        <h1 class="form-header">
          Are you sure you want to delete "<%= album.title %>"?
        </h1>
        <p>Once you have deleted this album, it cannot be recovered.</p>
        <div class="input-wrapper">
          <label for="password">Enter admin password to delete:</label>
          <input type="password" name="password" id="password" />
        </div>
        <div class="form-btns">
          <a class="form-btn" href="/albums/<%= album.id %>">Cancel</a>
          <button class="form-btn" type="submit">Submit</button>
        </div>
      </form>
    </div>
    <%- include('footer') %>
  </body>
</html>
