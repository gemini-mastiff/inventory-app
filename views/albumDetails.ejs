<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/styles.css" />
    <title><%= title %></title>
  </head>
  <body>
    <%- include('header') %>
    <div class="album-details-container">
      <img src="<%= album.cover_image_url %>" alt="<%= album.title %> cover" />
      <div class="album-item__info">
        <h3 class="album-item__title">
          <a class="album-item__link" href="/albums/<%= album.id %>"
            ><%= album.title %></a
          >
        </h3>
        <p class="album-item__release"><%= album.release_year %></p>
        <p class="album-item__artist"><%= album.artists %></p>
      </div>
    </div>
    <%- include('footer') %>
  </body>
</html>
